import 'package:flutter/material.dart';

/// Locale
const defaultLocale = Locale('en', 'US');

/// Durations
const dialogDuration = Duration(milliseconds: 700);
const popDuration = Duration(milliseconds: 300);

/// Appbar
const appbarAction = SizedBox(width: 20);

/// Button Height Min
const buttonMin = Size(50, 50);
const buttonMax = Size(double.infinity, 50);

/// Dimensions
const dialogPadding = 24.0;
